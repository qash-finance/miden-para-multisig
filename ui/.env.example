# API URL for the backend server
# For production: use qash-server (TypeScript/NestJS)
# For development/testing: can use either qash-server or rust server directly

# Production (recommended): Point to qash-server
VITE_API_URL=http://localhost:3000

# Development: Point directly to Rust server (if testing Rust server only)
# VITE_API_URL=http://localhost:4001
